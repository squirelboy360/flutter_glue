class ModalArguments {
  /// Controls whether the native header is shown
  static const String showNativeHeader = 'showNativeHeader';
  
  /// Sets the title in the native header (if enabled)
  static const String headerTitle = 'headerTitle';

  /// Controls whether to show the close button in native header
  static const String showCloseButton = 'showCloseButton';
}
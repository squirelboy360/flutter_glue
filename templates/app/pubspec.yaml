name: example_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  bloc: ^8.1.4
  forui: ^0.6.1
  http: ^1.2.2
  equatable: ^2.0.5
  go_router: ^14.4.1
  flutter_platform_alert: ^0.6.1
  flutter_bloc: ^8.1.6
  hydrated_bloc: ^9.1.5


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  test: ^1.16.0 # Add the test package for unit testing

flutter:
  uses-material-design: true